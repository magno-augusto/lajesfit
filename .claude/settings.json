{
  "permissions": {
    "allow": [
      "Bash(npx --no-install supabase db query --linked \"select id, name, category from public.foods where source='estimated' order by name;\" -o json)",
      "Read(//tmp/**)",
      "Bash(npx vercel *)",
      "Bash(npm run *)"
    ]
  }
}
